import React, { FC } from "react"
import { Button } from "antd"

import "./index.less"

const Home: FC = () => (
  <div>
    Hello world!
    <Button type="primary">Exit</Button>
  </div>
)

export default Home
